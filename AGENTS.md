# Agent 翻译与维护规范

本文件适用于仓库根目录及其全部子目录。所有 Agent 在翻译、润色、校对或修改本文档项目时，都必须先阅读本文件，并按当前仓库内容执行，不凭其他项目或旧版本经验推断。

## 1. 项目定位

- 本项目是 NeoForge 文档的非官方简体中文翻译与 VitePress 文档站。
- 中文文档位于 `docs/` 目录。
- 英文对照文档位于 `docs_en/` 目录。
- 统一术语表位于根目录 `GLOSSARY.md`。
- 翻译、润色和校对应以当前 `docs_en/` 中的英文原文、当前 `docs/` 中的既有中文写法，以及 `GLOSSARY.md` 中的术语规则为准。

## 2. 工作前检查

1. 先查看当前工作区状态，确认是否已有用户或其他 Agent 的修改。
2. 阅读待处理中文文件及其对应的英文原文文件。
3. 阅读待处理文件的相邻章节，确认本项目已有表达方式、标题层级、链接风格和术语习惯。
4. 阅读 `GLOSSARY.md`，逐项核对涉及的 NeoForge、Minecraft 和 Java 术语。
5. 如果用户只要求处理某个文件、章节或术语，必须严格限制修改范围；发现范围外问题可以说明，但不要擅自批量修改。

## 3. 基本翻译原则

- 翻译结果应符合中文阅读习惯，表达自然、准确、顺畅。
- 可以在不改变技术含义的前提下适当润色，避免生硬直译。
- 不得过度改写、扩写或删减原文含义。
- 不得为了中文流畅而改变原文中的技术边界、前置条件、警告语气或 API 行为描述。
- 同一概念在标题、正文、表格、链接文本、图表说明和提示框中应保持一致译法。

## 4. 已审核译文风格基准

用户已手工审核过 `.vitepress/config.mts` 侧边栏顺序中 `交互（Interactions）` 之前的中文文档。后续翻译和润色应优先参考这些页面的表达方式：

- `docs/gettingstarted/index.md`
- `docs/gettingstarted/modfiles.md`
- `docs/gettingstarted/structuring.md`
- `docs/gettingstarted/versioning.md`
- `docs/concepts/registries.md`
- `docs/concepts/sides.md`
- `docs/concepts/events.md`
- `docs/blocks/index.md`
- `docs/blocks/states.md`
- `docs/items/index.md`

具体风格要求：

- 文档主标题或核心概念首次出现时，可按需采用“中文译名（English Term）”形式；中文子标题如非必要，通常只写中文译名，不额外保留英文对照。
- 正文应像中文技术手册，而不是逐词对应英文原文。长句可以拆顺，因果、条件和建议关系应表达清楚。
- 保留教学型、说明型语气，可使用“你可以”“你需要”“应当”“请注意”等表达，但不要口语化或夸张。
- 区分强制规则、最佳实践和一般建议：按原文语气使用“必须”“应始终”“建议”“通常”“不建议”等词，不放大或削弱原意。
- 普通段落中的核心术语按 `GLOSSARY.md` 翻译；必要时保留英文对照，但不要让整段文字变成英文术语堆砌。
- 注意概念边界，尤其区分“方块 / 方块状态 / 方块实体”、“物品 / 物品堆叠 / 物品实体”、“物理端 / 逻辑端”、“事件总线 / 模组事件总线”等。
- 流程类内容应保持原文顺序，并用清晰中文说明每一步的触发条件、执行动作和结果。
- 示例、警告和提示中的语气应自然贴合中文读者，但不得改变原文的技术判断或风险等级。

## 5. 术语核对规则

- 翻译文档时必须仔细核对 `GLOSSARY.md`。
- `GLOSSARY.md` 中已有推荐译名的术语，普通说明文字必须使用推荐译名。
- NeoForge 相关术语遵守段落中的统一对照翻译规则：普通叙述中使用中文译名，必要时首次出现可写作“中文译名（English Term）”。
- 对于 `GLOSSARY.md` 中标明“避免使用”的译法，不得在对应概念上继续使用。
- 如果遇到 `GLOSSARY.md` 未覆盖的新术语，应先根据上下文确认含义；需要新增固定译名时，应同步补充术语表，除非用户明确要求只改正文。

## 6. 行内代码与技术标识符

以下内容默认保持原文，不翻译、不改名、不调整大小写：

- 使用反引号包裹的行内代码，例如 `Block`、`ItemStack`、`NeoForge.EVENT_BUS`、`ResourceLocation`。
- 类名、接口名、方法名、字段名、注解、包名和泛型签名。
- Registry Key、Resource Location、命名空间、注册名、资源路径、文件名和翻译键。
- 命令、配置键、JSON 键、枚举值和程序实际读取的字面量。
- Markdown 链接目标、引用定义、锚点和路径。

普通段落中的 NeoForge 术语需要按术语表翻译，但一旦它们出现在 `xxx` 这类反引号包裹区域中，就必须保持原文。

## 7. Minecraft 游戏术语

- Minecraft 游戏内概念、物品、方块、生物、界面和机制名称，应核对中文 Minecraft Wiki 或项目既有译法，确保使用中文玩家熟悉且准确的名称。
- 如果中文 Wiki 译名与 `GLOSSARY.md` 冲突，以 `GLOSSARY.md` 为当前项目规范；必要时向用户说明冲突并等待确认。
- 不要把 Minecraft 玩法术语、NeoForge API 术语和 Java 编程术语混用。例如普通概念中的“实体”和代码类型 `Entity` 应按上下文区分处理。

## 8. 代码块处理

- 代码块中的代码结构、类名、方法名、字段名、包名、JSON 键、资源路径、命令和其他程序字面量必须保持不变。
- 代码块中的说明性注释需要翻译为简体中文。
- 注释中涉及代码原文、API 名称、类名、方法名、字段名、路径、命令、JSON 键或资源标识符时，这些技术内容保持原文。
- 不得为了翻译注释而调整代码缩进、语法、导入、示例顺序或可运行性。

## 9. Markdown 与站点结构

- 保留原有 Markdown 结构，包括 frontmatter、标题层级、列表缩进、表格、脚注式链接、代码围栏和 VitePress 自定义容器。
- 不随意改动站点导航、目录结构或页面文件名。
- 修改标题时，应检查同页锚点、跨页链接和引用定义是否仍然有效。
- 新增或修改相对链接前，应确认目标文件实际存在，并按当前文件所在目录解析路径。
- 不把 Agent 的说明、待办、审校意见或工作记录写进正式文档，除非用户明确要求。

## 10. 校对与验证

完成修改后至少检查：

1. 目标范围内是否仍有旧译名、错译、漏译或与 `GLOSSARY.md` 冲突的表达。
2. 行内代码、代码块、链接目标、路径和技术标识符是否被误改。
3. 代码块中的说明性注释是否已翻译，且其中的代码原文保持不变。
4. Markdown 结构、标题层级、表格、提示框和链接引用是否保持正常。
5. 工作区差异是否只包含本次任务需要的内容。

## 11. 交付边界

- 不删除、覆盖或回退用户已有修改。
- 不擅自批量格式化、批量替换或重排未指定文件。
- 不擅自改变项目目标版本、文档范围或翻译风格。
- 未经用户要求，不提交、不推送、不创建分支。
- 交付说明应列出实际修改内容、涉及文件和完成的检查；如果有未验证项，应明确说明。
