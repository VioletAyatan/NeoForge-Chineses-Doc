# Agent 工作规范

本文件适用于仓库根目录及其全部子目录。所有参与本项目的 Agent 在读取、翻译、审校或修改文件前，都应先阅读本文件。

## 1. 项目定位

- 本项目是 **NeoForge 26.1 官方文档的非官方简体中文翻译**，使用 VitePress 构建。
- 主要文档位于 `docs/`，站点配置位于 `.vitepress/`。
- 除非用户明确要求，不得改变目标 NeoForge 版本、重写站点结构或扩大任务范围。
- 涉及版本、API 行为或文件路径时，应以当前仓库内容和 NeoForge 26.1 对应原文为准，不凭印象套用其他版本资料。

## 2. 工作前检查

1. 先运行 `git status --short`，识别用户或其他 Agent 已有的修改；不得覆盖、回退或顺手整理无关改动。
2. 阅读用户指定文件及其相邻章节，确认上下文、标题层级、链接引用和本页已有写法。
3. 阅读根目录的 `GLOSSARY.md`。其中的推荐译名、禁用译法和概念边界是本项目的统一标准。
4. 阅读 `README.md` 中的技术术语处理原则，并优先沿用目标文件附近已经稳定使用的格式。
5. 如果用户只要求修改某个文件、章节或术语，严格遵守该边界；发现其他问题可以报告，但不要擅自批量修改。

## 3. 翻译与术语规则

- 普通说明文字使用自然、准确的简体中文，优先表达技术含义，不机械逐词直译。
- `GLOSSARY.md` 中已有的术语必须统一使用。用户明确确立新译名时，应在完成目标修改的同时更新术语表。
- 同一概念在标题、正文、表格、链接文本和提示框中保持一致。
- 必要时可在首次出现处采用“中文译名（English Term）”，后文使用中文统一译名。
- 不得把不同层级的概念混为一谈。例如，条件被触发不等于成就完成，数据加载条件也不等于成就的完成条件。
- 不要仅因英文拼写相近就统一译名；应先确认它在 Minecraft、NeoForge 和 Java 语境中的实际含义。

## 4. 必须保留原文的内容

以下内容默认不得翻译、改名或调整大小写：

- fenced code block 及其中的代码和注释；
- 行内代码；
- 类名、接口名、方法名、字段名、包名和注解；
- Registry Key、Resource Location、命名空间、资源路径、文件名和翻译键；
- 命令、配置键、JSON 键、枚举值及其他程序实际读取的字面量；
- `Block`、`Item`、`ItemStack`、`Entity`、`Level`、`Registry`、`Codec`、`Holder` 等作为编程对象使用的名称。

因此，术语校正不得使用不区分上下文的全局替换。修改前后都应检查代码块、行内代码、路径和链接目标是否保持不变。

## 5. 文档编辑要求

- 保留原有 Markdown 结构，包括 frontmatter、标题层级、列表缩进、表格、脚注式链接、代码围栏和 VitePress 自定义容器。
- 只润色用户指定范围时，不重排章节，不重写无关段落，不改变示例代码。
- 修正标题后，应同步检查指向该标题的页内锚点和引用定义。
- 相对链接必须从当前文件所在目录解析；新增链接前确认目标文件实际存在。
- 外部链接、API 名称及示例中的标识符不得为了中文可读性而擅自改写。
- 不把自己的解释、工作记录、TODO 或审校备注混入正式译文，除非用户明确要求。

## 6. 术语表维护

- 术语表固定为根目录 `GLOSSARY.md`，不要另建内容重复的术语文件。
- 新条目至少应说明：英文原词、推荐译名、应避免的译法和适用边界。
- 派生词存在稳定译法时一并记录，例如根节点、树、选项卡、奖励等组合形式。
- “避免使用”只约束该英文概念的误译，不代表相应中文词在其他语境中一律禁用。
- 更新术语表不等于授权全仓库批量清理；是否扩展修改范围仍以用户要求为准。

## 7. 验证要求

每次修改后至少执行以下检查：

1. 使用精确搜索检查旧译名、错别字或其他目标问题是否仍残留在指定范围内。
2. 运行 `git diff --check`，排除空白错误和冲突标记。
3. 查看完整差异，确认没有修改代码块、技术标识符或用户的无关内容。
4. 涉及标题或链接时，检查引用定义、相对路径和锚点。
5. 条件允许时运行 `npm.cmd run docs:build`。在 PowerShell 中优先使用 `npm.cmd`，避免本机执行策略拦截 `npm.ps1`。

如果全站构建被既有错误阻断，应区分“本次引入的问题”和“仓库原有问题”，继续完成针对变更文件的静态检查，并在交付时准确说明阻塞位置，不得声称构建已经通过。

## 8. 修改与交付边界

- 不删除、覆盖或还原不属于当前任务的文件和修改。
- 不执行 `git reset --hard`、强制检出或其他破坏性操作。
- 未经用户要求，不提交、不推送、不创建分支，也不批量格式化整个仓库。
- 交付时列出实际修改的文件、核心校正内容和验证结果；存在未验证项或既有阻塞时必须明确说明。

## 9. 完成标准

任务只有在以下条件满足后才算完成：

- 用户指定范围内的修改已经落实；
- 术语符合 `GLOSSARY.md`，技术标识符保持原文；
- 未影响范围外内容或其他人的现有修改；
- 差异和链接已经复核，并完成与风险相称的验证；
- 最终说明与实际文件状态一致。
